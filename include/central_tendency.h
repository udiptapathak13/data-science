#ifndef _CENTRAL_TENDENCY_H
#define _CENTRAL_TENDENCY_H

#include <updef.h>
#include <stddef.h>

real_t getMean(real_t *, size_t);
real_t getMedian(real_t *, size_t);
real_t getVariance(real_t *, size_t);

#endif
